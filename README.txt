This project requires the flask Framework for Python. PIP install flask and then run 

python app.py -http [port]

in the project terminal, the website will then be accessible through localhost at the specified port.